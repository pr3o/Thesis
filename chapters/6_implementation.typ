#import "../config/thesis-config.typ": glpl, gl,
#pagebreak(to:"odd")

= Implementazione <cap:implementazione>

#text(style: "italic", [
    In questo capitolo descrivo come sono state implementate le scelte architetturali e di design illustrate nel capitolo precedente, illustrando le principali sfide affrontate durante lo sviluppo del progetto e come sono state superate.
])
#v(1em)