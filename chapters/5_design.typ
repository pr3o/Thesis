#import "../config/thesis-config.typ": glpl, gl,
#pagebreak(to:"odd")

= Progettazione <cap:progettazione>

#text(style: "italic", [
    In questo capitolo affronto la progettazione del sistema, descrivendo le scelte architetturali e di design che hanno guidato lo sviluppo del progetto.
])
#v(1em)